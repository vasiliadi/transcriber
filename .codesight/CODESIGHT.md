# transcriber — AI Context Map

> **Stack:** Python · Streamlit · Replicate · Google Gemini · yt-dlp · ffmpeg

> 0 routes | 0 models | 0 components | 17 functions | 4 env vars | 0 middleware

---

# Libraries

- `src/streamlit_app.py`
  - `download(url, mode)` — saves uploaded file or fetches audio from YouTube/URL/castro.fm
  - `compress_audio(audio_file_name, converted_file_name)` — ffmpeg: convert to mono ogg/opus 16kbps
  - `correct_transcription(transcription, post_processing)` — Gemini: fix spelling/punctuation (@cached)
  - `get_latest_model_version(model_name)` — fetch latest Replicate model version ID
  - `get_latest_prediction_output(sleep_time)` — poll Replicate until prediction output is ready
  - `detected_num_speakers(transcription, model)` — count unique speakers (@cached)
  - `process_diarization_for_incredibly_fast_whisper(transcription)` — group consecutive segments by speaker (@cached)
  - `process_whisper_diarization(audio_file_name)` — run whisper-diarization on Replicate
  - `process_incredibly_fast_whisper(audio_file_name, diarization, post_processing)` — run incredibly-fast-whisper on Replicate
  - `process_openai(audio_file_name)` — run openai/gpt-4o-transcribe on Replicate
  - `process_whisperx(audio_file_name, diarization)` — run whisperx on Replicate
  - `transcribe(model_name)` — dispatcher: routes to correct process_* function
  - `translate(text, target_language, chunks, sleep_time)` — Gemini translation (@cached)
  - `identify_speakers(transcription)` — Gemini: map SPEAKER_XX labels to real names (@cached)
  - `convert_to_minutes(seconds)` — format float seconds → "MM:SS" (@cached)
  - `clean_up()` — delete temp audio.mp3 and audio.ogg
  - `process_transcription()` — orchestrate compress → transcribe → display

---

# Config

## Environment Variables

- `GEMINI_API_KEY` **required** — Google Gemini client (`src/streamlit_app.py:18`)
- `HF_ACCESS_TOKEN` **required** — HuggingFace token for diarization models (`src/streamlit_app.py:48`)
- `PROXY` **required** — proxy for yt-dlp/requests (`src/streamlit_app.py:51`, read via `os.environ.get`)
- `REPLICATE_API_TOKEN` **required** — Replicate client (`src/streamlit_app.py:42`)

## Config Files

- `Dockerfile`
- `pyproject.toml`
- `compose.yaml`

---

_Generated by [codesight](https://github.com/Houseofmvps/codesight) — manually updated 2026-04-23_
