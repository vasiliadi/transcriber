spec:
  name: transcriber-app
  services:
  - name: transcriber-web-app
    git:
      branch: do-deploy
      repo_clone_url: https://github.com/vasiliadi/transcriber.git
    envs:
    - key: GEMINI_API_KEY
      scope: RUN_AND_BUILD_TIME
      type: SECRET
      # value: 
    - key: REPLICATE_API_TOKEN
      scope: RUN_AND_BUILD_TIME
      type: SECRET
      # value: 
